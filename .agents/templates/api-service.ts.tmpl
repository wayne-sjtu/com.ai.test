/**
 * {{xxx}} 模块接口调用封装
 * 统一处理基础 URL 与错误；放 src/services/ 下
 */
const BASE_URL = import.meta.env.VITE_API_BASE ?? "";

export interface {{Xxx}}Item {
  // 按实际响应结构定义
}

export async function fetch{{Xxx}}(): Promise<{{Xxx}}Item[]> {
  const res = await fetch(`${BASE_URL}/api/{{kebab-xxx}}`);
  if (!res.ok) {
    throw new Error(`请求失败: ${res.status}`);
  }
  return res.json();
}
